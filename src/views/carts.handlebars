<div>
    <span>ID del Carrito :  {{cid}}</span>
</div>

<div>
    <ul>
        <h4>Productos agregados al carrito</h4>
        </br>
        {{title}}
        {{#each products}}
        <li>
            {{this.title}} - {{this.quantity}}
        </li>
        {{/each}}
    </ul>
</div>
